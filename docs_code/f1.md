# 機能1: チャット/RAG機能 - 詳細コード解析

## 📋 概要

ユーザーの質問に対して**ベクトル検索 + LLM**で回答を生成する中核機能。  
以下の順序でデータと関数が呼び出される。

---

## 🔄 全体フロー図

```mermaid
flowchart TB
    subgraph Frontend["🌐 Frontend"]
        A["👤 ユーザー入力"]
        B["sendQuestion()"]
        C["apiPostJson('/chat')"]
        D["UI更新: messages, relatedFaqs"]
    end

    subgraph API["📡 API Layer"]
        E["POST /api/v1/chat"]
        F["post_chat()"]
    end

    subgraph Service["⚙️ chat_service.py"]
        G["handle_chat()"]
        H["_get_or_create_conversation()"]
        I["create_query()"]
        J["search_chunk_db()"]
        K["create_llm_answer()"]
        L["参照情報構築"]
    end

    subgraph FAISS["🗃️ faiss_service.py"]
        M["similarity_search_with_score()"]
        N["get_embeddings()"]
    end

    subgraph Gemini["🧠 gemini_service.py"]
        O["generate_structured()"]
    end

    subgraph DB["💾 MySQL"]
        P[("conversations")]
        Q[("messages")]
        R[("message_references")]
    end

    subgraph External["☁️ 外部API"]
        S["OpenAI Embeddings API"]
        T["Gemini API"]
    end

    A --> B --> C --> E --> F --> G
    G --> H --> P
    H --> G
    G --> Q
    G --> I --> O --> T
    T --> O --> I --> G
    G --> J --> M --> N --> S
    S --> N --> M --> J --> G
    G --> K --> O --> T
    T --> O --> K --> G
    G --> L --> R
    G --> F --> C --> D
```

---

## 📊 関数呼び出しシーケンス図

```mermaid
sequenceDiagram
    autonumber
    
    participant User as 👤 ユーザー
    participant Page as page.tsx
    participant API as api.ts
    participant Chat as chat.py
    participant Svc as chat_service.py
    participant FAISS as faiss_service.py
    participant Gemini as gemini_service.py
    participant DB as MySQL
    participant OpenAI as OpenAI API
    participant GeminiAPI as Gemini API

    User->>Page: 質問入力 + 送信
    
    Note over Page: sendQuestion(question)
    Page->>Page: setMessages([...prev, userMsg])
    Page->>Page: setLoading(true)
    
    Page->>API: apiPostJson("/chat", {query, session_id})
    API->>Chat: POST /api/v1/chat
    
    Note over Chat: post_chat(req, db)
    Chat->>Svc: handle_chat(db, user_input, session_id)
    
    rect rgb(255, 245, 238)
        Note over Svc,DB: Step 1: 会話取得/作成
        Svc->>Svc: _get_or_create_conversation()
        Svc->>DB: SELECT * FROM conversations WHERE session_id=?
        DB-->>Svc: Conversation | null
        alt 会話が存在しない
            Svc->>DB: INSERT INTO conversations
        end
    end
    
    rect rgb(255, 245, 238)
        Note over Svc,DB: Step 2: ユーザーメッセージ保存
        Svc->>DB: INSERT INTO messages (role=user)
    end
    
    rect rgb(238, 245, 255)
        Note over Svc,GeminiAPI: Step 3: クエリ変換
        Svc->>Svc: create_query(user_input)
        Svc->>Gemini: generate_structured(prompt, QueryResult)
        Gemini->>GeminiAPI: POST /models/gemini:generateContent
        GeminiAPI-->>Gemini: {"search_query": "..."}
        Gemini-->>Svc: QueryResult.search_query
    end
    
    rect rgb(238, 255, 238)
        Note over Svc,OpenAI: Step 4: ベクトル検索（Chunk）
        Svc->>Svc: search_chunk_db(user_query)
        Svc->>FAISS: similarity_search_with_score(chunk_dir)
        FAISS->>FAISS: get_embeddings()
        FAISS->>OpenAI: POST /embeddings
        OpenAI-->>FAISS: embedding vector
        FAISS->>FAISS: FAISS.load_local() + search
        FAISS-->>Svc: [(Document, distance), ...]
    end
    
    rect rgb(255, 238, 255)
        Note over Svc,GeminiAPI: Step 5: LLM回答生成
        Svc->>Svc: create_llm_answer(chunk_results, user_input)
        Svc->>Gemini: generate_structured(prompt, LLMAnswerResult)
        Gemini->>GeminiAPI: POST /models/gemini:generateContent
        GeminiAPI-->>Gemini: {"answer": "...", "referenced_pages": [...]}
        Gemini-->>Svc: LLMAnswerResult
    end
    
    rect rgb(255, 255, 238)
        Note over Svc,DB: Step 6-7: 回答・参照保存
        Svc->>Svc: Build references from LLM output
        Svc->>DB: INSERT INTO messages (role=assistant)
        Svc->>DB: INSERT INTO message_references
    end
    
    Svc-->>Chat: (answer, references)
    Chat-->>API: ChatResponse
    API-->>Page: ChatResponse

    Page->>Page: setMessages([...prev, assistantMsg])
    Page->>Page: setLoading(false)
    Page-->>User: 回答表示
```

**補足**: FAQ検索は別エンドポイント `/chat/related-faqs` で並列実行され、フロントエンドで結果をマージして表示

---

## 📦 データフロー詳細

### 入力 → 出力の変換

```mermaid
flowchart LR
    subgraph Input["📥 入力"]
        A["query: string"]
        B["session_id: string"]
    end
    
    subgraph Transform["🔄 変換処理"]
        C["自然言語→検索キーワード"]
        D["キーワード→ベクトル"]
        E["ベクトル→類似ドキュメント"]
        F["ドキュメント→回答テキスト"]
    end
    
    subgraph Output["📤 出力"]
        G["answer: string"]
        H["faq_results: FAQResult[]"]
        I["references: Reference[]"]
    end
    
    A --> C --> D --> E --> F --> G
    E --> H
    F --> I
```

### 各ステップのデータ形式

| Step | 関数 | 入力 | 出力 |
|:----:|------|------|------|
| 1 | `_get_or_create_conversation()` | `session_id: str` | `Conversation` |
| 2 | DB INSERT | `Message(role=user)` | - |
| 3 | `create_query()` | `user_input: str` | `search_query: str` |
| 4 | `search_chunk_db()` | `user_query: str` | `ChunkResult[]` |
| 5 | `create_llm_answer()` | `ChunkResult[], user_input` | `LLMAnswerResult` |
| 6-7 | DB INSERT | `Message, MessageReference` | - |

---

## 🏗️ 主要クラス/型定義

```mermaid
classDiagram
    class ChatRequest {
        +query: str
        +session_id: str
    }
    
    class ChatResponse {
        +faq_results: FAQResult[]
        +answer: str
        +references: Reference[]
    }
    
    class QueryResult {
        +search_query: str
    }
    
    class ChunkResult {
        +page_id: str
        +page_number: int
        +search_query: str
        +detail_description: str
        +image_url: str
        +relevance_score: float
    }
    
    class FAQSearchResult {
        +id: str
        +title: str
        +search_query: str
        +answer: str
        +page_id: Optional[str]
        +page_number: Optional[int]
        +image_url: Optional[str]
        +relevance_score: float
    }
    
    class LLMAnswerResult {
        +answer: str
        +referenced_pages: List[int]
    }
    
    class Reference {
        +page_id: str
        +page_number: int
        +image_url: str
        +relevance_score: float
        +is_primary: bool
    }
    
    ChatRequest --> QueryResult : create_query()
    QueryResult --> ChunkResult : search_chunk_db()
    QueryResult --> FAQSearchResult : search_faq_db()
    ChunkResult --> LLMAnswerResult : create_llm_answer()
    LLMAnswerResult --> Reference : 参照構築
    FAQSearchResult --> ChatResponse : via /related-faqs
    Reference --> ChatResponse
```

---

## 🔍 関数詳細

### 1. `handle_chat()` - メインエントリポイント

```python
# ファイル: chat_service.py:174-237
async def handle_chat(db, *, user_input, session_id):
    # Step 1: 会話取得/作成
    conv = await _get_or_create_conversation(db, session_id)

    # Step 2: ユーザーメッセージ保存
    user_msg = Message(...)
    db.add(user_msg)

    # Step 3: クエリ変換
    user_query = await create_query(user_input)

    # Step 4: ベクトル検索（チャンクのみ）
    chunk_results = await search_chunk_db(user_query)

    # Step 5: LLM回答生成
    llm = await create_llm_answer(chunk_results, user_input)

    # Step 6-7: 参照構築 & 保存
    references = [...]  # LLMが選んだページ番号をマッチング
    assistant_msg = Message(...)
    db.add(assistant_msg)
    # message_references も保存

    return llm.answer, references
```

### 2. `create_query()` - クエリ変換

```mermaid
flowchart LR
    A["駐車場の料金を教えてください"] --> B["Gemini API"]
    B --> C["駐車場 料金"]
    
    style A fill:#ffe0e0
    style C fill:#e0ffe0
```

**プロンプト構造**:
- 入力: ユーザーの自然言語質問
- 指示: 短いキーワード/フレーズに変換
- 出力: `QueryResult { search_query: str }`

### 3. `search_chunk_db()` / `search_faq_db()` - ベクトル検索

```mermaid
flowchart TB
    A["検索クエリ"] --> B["OpenAI Embeddings"]
    B --> C["ベクトル化"]

    C --> D["Chunk FAISS Index"]
    C --> E["FAQ FAISS Index"]

    D --> F["ChunkResult[]"]
    E --> G["FAQSearchResult[]"]
```

**検索パラメータ**:
- `search_chunk_db()`: `k=5` (上位5件) - `/chat` エンドポイントで使用
- `search_faq_db()`: `k=3` (上位3件) - `/chat/related-faqs` エンドポイントで使用

**注意**: チャット回答生成とFAQ検索は別々のエンドポイントで実行される

### 4. `create_llm_answer()` - 回答生成

**コンテキスト構築**:
```
【ページ1】
詳細説明テキスト...

【ページ3】
詳細説明テキスト...
```

**出力形式**:
```json
{
  "answer": "回答テキスト",
  "referenced_pages": [1, 3]
}
```

### 5. `similarity_search_with_score()` - FAISS検索

```mermaid
flowchart LR
    A["クエリ文字列"] --> B["OpenAIEmbeddings"]
    B --> C["text-embedding-3-large"]
    C --> D["ベクトル (3072次元)"]
    D --> E["FAISS IndexFlatL2"]
    E --> F["[(Document, distance), ...]"]
    F --> G["_distance_to_relevance()"]
    G --> H["relevance_score (0~1)"]
```

**距離 → 関連度変換**:
```python
def _distance_to_relevance(distance):
    return 1.0 / (1.0 + distance)
```

---

## 💾 DB操作タイムライン

```mermaid
gantt
    title DB操作タイミング
    dateFormat X
    axisFormat %s
    
    section 読み取り
    SELECT conversations    :0, 1
    
    section 書き込み
    INSERT conversations (必要時) :1, 2
    INSERT messages (user)        :2, 3
    INSERT messages (assistant)   :6, 7
    INSERT message_references     :7, 8
    
    section 外部API
    Gemini (create_query)         :3, 4
    OpenAI Embeddings             :4, 5
    Gemini (create_llm_answer)    :5, 6
```

---

## 📁 ファイル対応表

| ファイル | 主要関数 | 責務 |
|---------|---------|------|
| [page.tsx](file:///Users/t/Projects/RAG/tenant-ai-assistant-mvp/frontend/app/page.tsx#L61-127) | `sendQuestion()` | UI状態管理、API呼び出し |
| [api.ts](file:///Users/t/Projects/RAG/tenant-ai-assistant-mvp/frontend/lib/api.ts#L19-33) | `apiPostJson()` | HTTPリクエスト |
| [chat.py](file:///Users/t/Projects/RAG/tenant-ai-assistant-mvp/backend/app/api/v1/endpoints/chat.py#L16-23) | `post_chat()` | APIエンドポイント |
| [chat_service.py](file:///Users/t/Projects/RAG/tenant-ai-assistant-mvp/backend/app/services/chat_service.py#L174-237) | `handle_chat()` | ビジネスロジック統括 |
| [chat_service.py](file:///Users/t/Projects/RAG/tenant-ai-assistant-mvp/backend/app/services/chat_service.py#L54-71) | `create_query()` | クエリ変換 |
| [chat_service.py](file:///Users/t/Projects/RAG/tenant-ai-assistant-mvp/backend/app/services/chat_service.py#L74-96) | `search_chunk_db()` | Chunkベクトル検索 |
| [chat_service.py](file:///Users/t/Projects/RAG/tenant-ai-assistant-mvp/backend/app/services/chat_service.py#L99-123) | `search_faq_db()` | FAQベクトル検索 |
| [chat_service.py](file:///Users/t/Projects/RAG/tenant-ai-assistant-mvp/backend/app/services/chat_service.py#L126-158) | `create_llm_answer()` | 回答生成 |
| [faiss_service.py](file:///Users/t/Projects/RAG/tenant-ai-assistant-mvp/backend/app/services/faiss_service.py#L52-57) | `similarity_search_with_score()` | FAISS検索 |
| [gemini_service.py](file:///Users/t/Projects/RAG/tenant-ai-assistant-mvp/backend/app/services/gemini_service.py#L53-78) | `generate_structured()` | LLM構造化出力 |

---

## 🎯 コードリーディングのポイント

1. **エントリポイント**: `handle_chat()` から読み始める
2. **データ変換の流れ**: 自然言語 → キーワード → ベクトル → ドキュメント → 回答
3. **非同期処理**: すべての関数が `async` で、外部API呼び出しは並列化可能
4. **距離 → スコア変換**: FAISSは「小さいほど類似」なので変換が必要
5. **参照マッチング**: LLMが出力したページ番号と検索結果をマッチング
