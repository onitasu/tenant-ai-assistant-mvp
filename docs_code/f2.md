# 機能2: ドキュメント処理 - 詳細コード解析

## 概要

PDF/PPTXをアップロード → ページ分割 → OCR/画像解析 → インデックス登録を行うドキュメント処理機能。
SSE（Server-Sent Events）でリアルタイムに処理進捗をストリーミング。

---

## 全体フロー図

```mermaid
flowchart TB
    subgraph Frontend["Frontend"]
        A["ファイル選択"]
        B["POST /documents/upload"]
        C["SSE進捗受信"]
        D["完了通知表示"]
    end

    subgraph API["API Layer"]
        E["upload_document()"]
        F["StreamingResponse"]
    end

    subgraph Processor["document_processor.py"]
        G["process_uploaded_document_with_progress()"]
        H["_soffice_convert_to_pdf()"]
        I["fitz.open() - PDF読込"]
        J["_render_page_to_png()"]
        K["_extract_page_with_gemini()"]
    end

    subgraph Gemini["gemini_service.py"]
        L["generate_structured()"]
        M["part_from_bytes()"]
    end

    subgraph Storage["Storage"]
        N[("MySQL - Document, Page")]
        O[("FAISS Index")]
        P["storage/images/"]
        Q["storage/uploads/"]
    end

    subgraph External["外部API"]
        R["Gemini Vision API"]
        S["OpenAI Embeddings"]
    end

    A --> B --> E --> F
    F --> C --> D
    E --> G
    G --> H --> I
    I --> J --> P
    J --> K --> L --> M --> R
    R --> L --> K --> G
    G --> N
    G --> O --> S
    Q --> I
```

---

## 関数呼び出しシーケンス図

```mermaid
sequenceDiagram
    autonumber

    participant Client as Client
    participant API as documents.py
    participant Proc as document_processor.py
    participant Gemini as gemini_service.py
    participant FAISS as faiss_service.py
    participant DB as MySQL
    participant GeminiAPI as Gemini API
    participant OpenAI as OpenAI API
    participant FS as FileSystem

    Client->>API: POST /upload (file, title)

    Note over API: upload_document()

    rect rgb(255, 245, 238)
        Note over API,FS: Step 1: ファイル保存
        API->>API: _infer_file_type(filename)
        API->>FS: write_bytes(upload_path)
        API->>DB: INSERT Document(status=pending)
    end

    API->>Client: StreamingResponse開始
    API->>Proc: process_uploaded_document_with_progress()

    rect rgb(238, 245, 255)
        Note over Proc,DB: Step 2: 処理開始
        Proc->>DB: UPDATE status=processing
        Proc-->>Client: SSE: {"status": "processing", "message": "ファイルを変換中..."}
    end

    rect rgb(255, 238, 238)
        Note over Proc,FS: Step 3: PPTX→PDF変換 (必要時)
        alt file_type in {pptx, ppt}
            Proc->>Proc: _soffice_convert_to_pdf()
            Proc->>FS: soffice --headless --convert-to pdf
        end
    end

    Proc->>Proc: fitz.open(pdf_path)
    Proc-->>Client: SSE: {"total_pages": N, "current_page": 0}

    loop 各ページ (i = 0 to N-1)
        rect rgb(238, 255, 238)
            Note over Proc,GeminiAPI: Step 4-5: ページ処理
            Proc-->>Client: SSE: {"current_page": i+1}

            Proc->>Proc: _render_page_to_png(doc, i)
            Proc->>FS: write PNG to storage/images/documents/{doc_id}/

            Proc->>Gemini: _extract_page_with_gemini(image_bytes)
            Gemini->>Gemini: part_from_bytes(data, "image/png")
            Gemini->>GeminiAPI: generate_structured(PageExtraction)
            GeminiAPI-->>Gemini: {title, search_query, page_text, img_description}
            Gemini-->>Proc: PageExtraction

            Proc->>DB: INSERT Page(...)
        end
    end

    rect rgb(255, 255, 238)
        Note over Proc,OpenAI: Step 6: インデックス構築
        Proc-->>Client: SSE: {"message": "インデックスを更新中..."}
        Proc->>FAISS: add_texts_to_index(texts, metadatas)
        FAISS->>OpenAI: POST /embeddings
        OpenAI-->>FAISS: embedding vectors
        FAISS->>FS: save FAISS index
    end

    rect rgb(238, 238, 255)
        Note over Proc,DB: Step 7: 完了
        Proc->>DB: UPDATE status=completed
    end

    Proc-->>API: 処理完了
    API-->>Client: SSE: {"status": "completed", "document": {...}}
```

---

## データフロー詳細

### 入力 → 出力の変換

```mermaid
flowchart LR
    subgraph Input["入力"]
        A["PDF/PPTX ファイル"]
        B["title: string"]
    end

    subgraph Transform["変換処理"]
        C["PPTX→PDF変換"]
        D["PDF→ページ画像"]
        E["画像→構造化テキスト"]
        F["テキスト→ベクトル"]
    end

    subgraph Output["出力"]
        G["Document レコード"]
        H["Page レコード (N件)"]
        I["FAISS Index 更新"]
        J["PNG画像 (N枚)"]
    end

    A --> C --> D --> E --> F
    B --> G
    E --> H
    F --> I
    D --> J
```

### 各ステップのデータ形式

| Step | 関数 | 入力 | 出力 |
|:----:|------|------|------|
| 1 | `upload_document()` | `UploadFile, title` | `Document(status=pending)` |
| 2 | `process_uploaded_document_with_progress()` | `Document, input_file_path` | SSE stream |
| 3 | `_soffice_convert_to_pdf()` | `Path (PPTX)` | `Path (PDF)` |
| 4 | `_render_page_to_png()` | `fitz.Document, page_index` | `bytes (PNG)` |
| 5 | `_extract_page_with_gemini()` | `bytes (PNG)` | `PageExtraction` |
| 6 | `add_texts_to_index()` | `texts[], metadatas[]` | FAISS Index |

---

## 主要クラス/型定義

```mermaid
classDiagram
    class Document {
        +id: str
        +title: str
        +file_path: str
        +file_type: str
        +total_pages: int
        +status: DocumentStatus
        +created_at: datetime
    }

    class DocumentStatus {
        <<enumeration>>
        pending
        processing
        completed
        error
    }

    class Page {
        +id: str
        +document_id: str FK
        +page_number: int
        +title: Optional[str]
        +page_text: Optional[str]
        +search_query: str
        +img_description: Optional[str]
        +image_url: str
    }

    class PageExtraction {
        +title: Optional[str]
        +search_query: str
        +page_text: Optional[str]
        +img_description: Optional[str]
    }

    class SSEProgress {
        +status: str
        +message: str
        +total_pages: int
        +current_page: int
    }

    Document --> DocumentStatus
    Document "1" --> "*" Page : has
    PageExtraction --> Page : maps to
```

---

## 関数詳細

### 1. `upload_document()` - APIエンドポイント

```python
# ファイル: documents.py:35-92
@router.post("/upload", status_code=status.HTTP_201_CREATED)
async def upload_document(db, file, title):
    # ファイルタイプ検証
    file_type = _infer_file_type(file.filename)  # pdf, pptx, ppt のみ許可

    # ファイル保存
    upload_path = settings.uploads_dir / f"{document_id}.{file_type}"
    upload_path.write_bytes(content)

    # Document レコード作成
    doc = Document(id=document_id, title=safe_title, ...)
    db.add(doc)

    # SSEストリーミング開始
    return StreamingResponse(generate_progress(), media_type="text/event-stream")
```

### 2. `process_uploaded_document_with_progress()` - 処理本体

```mermaid
flowchart TB
    A["開始"] --> B["status = processing"]
    B --> C{"PPTX?"}
    C -->|Yes| D["_soffice_convert_to_pdf()"]
    C -->|No| E["fitz.open()"]
    D --> E
    E --> F["ループ: 各ページ"]
    F --> G["_render_page_to_png()"]
    G --> H["_extract_page_with_gemini()"]
    H --> I["Page レコード作成"]
    I --> J{"次ページ?"}
    J -->|Yes| F
    J -->|No| K["add_texts_to_index()"]
    K --> L["status = completed"]
    L --> M["終了"]

    style D fill:#ffe0e0
    style H fill:#e0e0ff
    style K fill:#e0ffe0
```

### 3. `_soffice_convert_to_pdf()` - LibreOffice変換

```python
# ファイル: document_processor.py:34-53
def _soffice_convert_to_pdf(input_path: Path, out_dir: Path) -> Path:
    cmd = [
        "soffice",
        "--headless",
        "--convert-to", "pdf",
        "--outdir", str(out_dir),
        str(input_path),
    ]
    subprocess.run(cmd, check=True)
    return out_dir / (input_path.stem + ".pdf")
```

**依存関係**: LibreOffice (`soffice`) がシステムにインストールされている必要あり

### 4. `_render_page_to_png()` - ページレンダリング

```python
# ファイル: document_processor.py:56-62
def _render_page_to_png(doc: fitz.Document, page_index: int, out_path: Path, dpi=200):
    page = doc.load_page(page_index)
    pix = page.get_pixmap(dpi=dpi)
    img_bytes = pix.tobytes("png")
    out_path.write_bytes(img_bytes)
    return img_bytes
```

**パラメータ**:
- `dpi=200`: 高解像度でOCR精度を確保

### 5. `_extract_page_with_gemini()` - Gemini Vision解析

```mermaid
flowchart LR
    A["PNG画像"] --> B["part_from_bytes()"]
    B --> C["generate_structured()"]
    C --> D["Gemini Vision API"]
    D --> E["PageExtraction"]

    style A fill:#ffe0e0
    style E fill:#e0ffe0
```

**プロンプト構造**:
```
この画像はテナント向けマニュアルの1ページです。
以下の情報を漏れなく抽出してください。
1. ベクトル検索に使用する検索クエリ（短いキーワード/フレーズ）
2. OCRで抽出した本文テキスト
3. 図表/地図/フロー図の説明
4. ページの見出し/タイトル
```

**出力スキーマ**: `PageExtraction`
```json
{
  "title": "駐車場利用ガイド",
  "search_query": "駐車場 利用 ルール 料金",
  "page_text": "駐車場のご利用について...",
  "img_description": "駐車場レイアウト図。A棟、B棟の位置と..."
}
```

**ファイルサイズによる分岐**:
- 18MB未満: `part_from_bytes()` でインライン送信
- 18MB以上: `upload_file()` で Files API 使用

### 6. `add_texts_to_index()` - FAISSインデックス追加

```mermaid
flowchart LR
    A["texts[]"] --> B["OpenAI Embeddings"]
    B --> C["ベクトル化"]
    C --> D["既存Index読込"]
    D --> E["add_texts()"]
    E --> F["save_local()"]

    style A fill:#ffe0e0
    style F fill:#e0ffe0
```

**メタデータ構造**:
```json
{
  "page_id": "uuid",
  "document_id": "uuid",
  "page_number": 1,
  "detail_description": "【タイトル】...\n【本文テキスト】...",
  "image_url": "/static/images/documents/{doc_id}/page_1.png"
}
```

---

## ストレージ構造

```
storage/
├── uploads/          # アップロードファイル
│   └── {document_id}.pdf
├── images/           # ページ画像
│   └── documents/
│       └── {document_id}/
│           ├── page_1.png
│           ├── page_2.png
│           └── ...
├── faiss/            # FAISSインデックス
│   ├── chunk_faiss/
│   │   ├── index.faiss
│   │   └── index.pkl
│   └── faq_faiss/
│       ├── index.faiss
│       └── index.pkl
└── tmp/              # 一時ファイル (PPTX→PDF変換)
```

---

## SSE進捗イベント

```mermaid
sequenceDiagram
    participant S as Server
    participant C as Client

    S->>C: {"status": "processing", "message": "ファイルを変換中..."}
    S->>C: {"status": "processing", "total_pages": 10, "current_page": 0}

    loop 各ページ
        S->>C: {"status": "processing", "current_page": 1, "total_pages": 10}
        Note over S: ページ処理...
        S->>C: {"status": "processing", "current_page": 2, "total_pages": 10}
    end

    S->>C: {"status": "processing", "message": "インデックスを更新中..."}
    S->>C: {"status": "completed", "document": {...}}
```

---

## エラーハンドリング

```mermaid
flowchart TB
    A["処理開始"] --> B{"エラー発生?"}
    B -->|No| C["status = completed"]
    B -->|Yes| D["rollback()"]
    D --> E["status = error"]
    E --> F["SSE: error message"]

    style D fill:#ffe0e0
    style E fill:#ffe0e0
```

**エラー時の動作**:
1. DBトランザクションをロールバック
2. `Document.status` を `error` に更新
3. SSEでエラーメッセージを送信

---

## 削除処理

```mermaid
flowchart TB
    A["DELETE /documents/{id}"] --> B["ファイル削除"]
    B --> C["アップロードファイル"]
    B --> D["画像フォルダ"]
    C --> E["Document削除 (CASCADE)"]
    D --> E
    E --> F["rebuild_chunk_index()"]
    F --> G["FAISSインデックス再構築"]
```

**カスケード削除**: `Document` 削除時に関連する `Page` も自動削除

---

## ファイル対応表

| ファイル | 主要関数 | 責務 |
|---------|---------|------|
| [documents.py](file:///Users/t/Projects/RAG/tenant-ai-assistant-mvp/backend/app/api/v1/endpoints/documents.py#L35-92) | `upload_document()` | アップロードAPI、SSEストリーミング |
| [documents.py](file:///Users/t/Projects/RAG/tenant-ai-assistant-mvp/backend/app/api/v1/endpoints/documents.py#L120-147) | `delete_document()` | ドキュメント削除 + インデックス再構築 |
| [document_processor.py](file:///Users/t/Projects/RAG/tenant-ai-assistant-mvp/backend/app/services/document_processor.py#L127-238) | `process_uploaded_document_with_progress()` | ドキュメント処理本体 |
| [document_processor.py](file:///Users/t/Projects/RAG/tenant-ai-assistant-mvp/backend/app/services/document_processor.py#L34-53) | `_soffice_convert_to_pdf()` | PPTX→PDF変換 |
| [document_processor.py](file:///Users/t/Projects/RAG/tenant-ai-assistant-mvp/backend/app/services/document_processor.py#L56-62) | `_render_page_to_png()` | ページ画像生成 |
| [document_processor.py](file:///Users/t/Projects/RAG/tenant-ai-assistant-mvp/backend/app/services/document_processor.py#L72-110) | `_extract_page_with_gemini()` | Gemini Vision OCR/解析 |
| [gemini_service.py](file:///Users/t/Projects/RAG/tenant-ai-assistant-mvp/backend/app/services/gemini_service.py#L53-78) | `generate_structured()` | 構造化出力生成 |
| [indexer.py](file:///Users/t/Projects/RAG/tenant-ai-assistant-mvp/backend/app/services/indexer.py#L25-51) | `rebuild_chunk_index()` | FAISSインデックス全再構築 |

---

## コードリーディングのポイント

1. **エントリポイント**: `upload_document()` から読み始める
2. **SSEパターン**: `StreamingResponse` + `async generator` で進捗配信
3. **PyMuPDF (fitz)**: PDFのページ分割・画像化に使用
4. **Gemini Vision**: 画像からの構造化データ抽出（OCR + 理解）
5. **インクリメンタル更新**: `add_texts_to_index()` で既存インデックスに追加
6. **削除時は全再構築**: `rebuild_chunk_index()` で整合性を保証
